import React from 'react';
export interface AuthLayoutProps {
    children: React.ReactNode;
}
declare const AuthLayout: ({ children }: AuthLayoutProps) => React.JSX.Element;
export default AuthLayout;
